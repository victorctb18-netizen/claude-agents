name: install
on: [push, pull_request]
jobs:
  idempotente:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 24 }
      - run: bash test/install.sh
      - run: bash test/merge.sh
